vaec:
  className: VAECdataset
  data_path: ${dataset.root}/vaec/datasets/
  # annotation_path: null
  img_size: null
  train:
    dataset_type: train
    data_path: analogy_train.hy
    # TODO: allow multiple or treat as separate datasets?
    # - analogy_scale_train.hy
    # annotation_path: ${dataset.vaec.annotation_path}
    img_size: ${dataset.vaec.img_size}
  val:
    dataset_type: analogy_test1.hy
      # TODO: allow multiple or treat as separate datasets?
      # TODO: split into val and test?
      # - analogy_scale_test1.hy
    data_path: ${dataset.vaec.data_path}
    # annotation_path: ${dataset.vaec.annotation_path}
    img_size: ${dataset.vaec.img_size}
  test:
    dataset_type: analogy_test1.hy
      # TODO: allow multiple or treat as separate datasets?
      # TODO: split into val and test?
      # - analogy_scale_test1.hy
    data_path: ${dataset.vaec.data_path}
    # annotation_path: ${dataset.vaec.annotation_path}
    img_size: ${dataset.vaec.img_size}
