bongard_logo:
  className: LOGOdataset
  data_path: ${dataset.root}/bongard_logo/ShapeBongard_V2/
  annotation_path: ${dataset.root}/bongard_logo/ShapeBongard_V2/ShapeBongard_V2_split.json
  img_size: null
  train:
    dataset_type: train
    data_path: ${dataset.bongard_logo.data_path}
    annotation_path: ${dataset.bongard_logo.annotation_path}
    img_size: ${dataset.bongard_logo.img_size}
  val:
    dataset_type: val
    data_path: ${dataset.bongard_logo.data_path}
    annotation_path: ${dataset.bongard_logo.annotation_path}
    img_size: ${dataset.bongard_logo.img_size}
  test:
    dataset_type: test_ff
      # TODO: allow multiple dataset types (requires change in dataset class)?
      # - test_hd_comb
      # - test_bd
      # - test_hd_novel
    data_path: ${dataset.bongard_logo.data_path}
    annotation_path: ${dataset.bongard_logo.annotation_path}
    img_size: ${dataset.bongard_logo.img_size}
